using System;
using System.Collections.Generic;

public class IComparable<T>
{
    private List<T> items;
    public IComparable()
    {
        items = new List<T>();
    }
    public IComparable(IEnumerable<T> initialItems)
    {
        items = new List<T>(initialItems);
    }
    public void Add(T item)
    {
        items.Add(item);
    }
    public bool Remove(T item)
    {
        return items.Remove(item);
    }
    public void RemoveByIndex(int index)
    {
        items[index] = default;
    }
    public T this[int index]
    {
        get { return items[index]; }
        set { items[index] = value; }
    }
    public int Count
    {
        get { return items.Count; }
    }
    public void Clear()
    {
        items.Clear();
    }
    public void Display()
    {
        for (int i = 0; i < items.Count; i++)
        {
            Console.WriteLine(items[i]);
        }
    }
}

class ResurcesDepartament: IComparable<ResurcesDepartament>
{
    private string surname;
    private string initials;
    private string date;
    private string salary;

    public ResurcesDepartament(string surname, string initials, string date, string salary)
    {
        this.surname = surname;
        this.initials = initials;
        this.date = date;
        this.salary = salary;
    }

    public string Surname
    {
        get { return surname; }
        set { surname = value; }
    }
    public string Initials
    {
        get { return initials; }
        set { initials = value; }
    }
    public string Date
    {
        get { return date; }
        set { date = value; }
    }
    public string Salary
    {
        get { return salary; }
        set { salary = value; }
    }

    public override string ToString()
    {
        return $"{Surname}\t{Initials}\t\t{Date}\t\t{Salary}";
    }
}

class Program
{
    static void Main()
    {
        Console.OutputEncoding = System.Text.Encoding.UTF8;

        IComparable<ResurcesDepartament> collection1 = new IComparable<ResurcesDepartament>();
        ResurcesDepartament a1 = new ResurcesDepartament("Іванов\t", "І.І.", "1975", "517.50");
        ResurcesDepartament b1 = new ResurcesDepartament("Петреноко", "П.П.", "1956", "219.10");
        ResurcesDepartament c1 = new ResurcesDepartament("Паніковський", "М.С.", "1967", "300.00");
        collection1.Add(a1);
        collection1.Add(b1);
        collection1.Add(c1);


        IComparable<ResurcesDepartament> collection2 = new IComparable<ResurcesDepartament>();
        ResurcesDepartament a2 = new ResurcesDepartament("Білоус\t", "І.І.", "1972", "717.50");
        ResurcesDepartament b2 = new ResurcesDepartament("Смотриковський", "П.П.", "1986", "119.10");
        ResurcesDepartament c2 = new ResurcesDepartament("Лувчук\t", "М.С.", "1997", "600.00");
        collection2.Add(a2);
        collection2.Add(b2);
        collection2.Add(c2);


        Console.WriteLine("Surname:\tInitials:\tBirthday:\tSalary:");
        collection1.Display();

        Console.WriteLine(collection1[0].ToString());

    }
}
